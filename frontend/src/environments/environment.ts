export const environment = {
  production: false,
  // Toggle demo mode for mocking API responses via DemoInterceptor
  useDemo: true,
  // Base URL for API; in development requests should be proxied via /api (see proxy.conf.json)
  apiBaseUrl: '/api',
};
